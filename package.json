{
  "license": "MIT",
  "workspaces": [
    "client",
    "map",
    "options",
    "scripts"
  ],
  "scripts": {
    "watch": "lerna run watch --stream",
    "build": "lerna run build && zip-build extension . -t arkadia-extension.%EXT%"
  },
  "devDependencies": {
    "lerna": "^8.2.1",
    "zip-build": "^1.8.0"
  }
}
